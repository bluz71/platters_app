import React from 'react';
import { HashRouter } from "react-router-dom";
import Layout from './components/Layout';

const App = () => (
  <HashRouter>
    <Layout />
  </HashRouter>
)

export default App;
